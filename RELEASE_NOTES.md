# pyside6-webusb を PyPI で pip install できるようにする対応

対象リポジトリ: https://github.com/steck0714/Pyside6-webusb
対象タグ: `v0.0.0` 〜 `v0.0.4b3`(全18タグ)

## 1. 調査でわかった問題

リポジトリを全タグぶんチェックアウトして調べたところ、そのままではPyPIに上げても
正しく `pip install` できない、次の2種類の問題がありました。

### 問題A: `pyproject.toml` のversionがタグと1つズレている

ほぼ全タグで、`pyproject.toml`(および `__init__.py`/`_version.py` 内の
`__version__`)の値が、**そのタグ自身ではなく1つ前のタグの番号のまま**でした。
（タグを打ってから version を上げる、という順序で作業されていたためと見られます。)

例: タグ `v0.0.4` を取得しても、中身の `pyproject.toml` は `version = "0.0.3b0"` の
まま。このままPyPIに上げると `pip install pyside6-webusb==0.0.4` は「存在しない
バージョン」としてエラーになります。

### 問題B: `a` / `b` 付きタグの命名が PEP 440(PyPIのバージョン規格)と衝突する

`v0.0.4` → `v0.0.4a` → `v0.0.4b` → `v0.0.4b1〜b3` のように、**無印バージョンを
先に、a/b付きを後から**タグ打ちする運用でした。ところがPyPIの規格(PEP 440)では
`0.0.4a0` `0.0.4b0` のような表記は「0.0.4の**プレリリース**(0.0.4より前)」という
意味になり、**無印の `0.0.4` の方が新しいと解釈されてしまいます**。

このままPyPIに上げると、一番新しいはずの `v0.0.4b3` よりも古い `v0.0.4` の方が
優先され、バージョン指定なしの `pip install pyside6-webusb` が**最新ではなく
古いコードを取得してしまう**という、ご要望の「最新のリリースをインストールできる
ように」に反する結果になることが分かりました。

### 問題C: `v0.0.0` にはコードが存在しない

最初のタグ `v0.0.0` の中身は `LICENSE` と `README.md` のみで、Pythonコードが
一切ありません。パッケージとして配布するものがないため、**`v0.0.0` はPyPI化の
対象から除外**しました(コードが入ったのは次の `v0.0.0a` からです)。

### 問題D: `v0.0.0a` に `pyproject.toml` が存在しない

コード自体は `v0.0.0a` から存在しますが、パッケージ化に必要な `pyproject.toml`
がまだ作られていませんでした。既存コードは変更せず、最小限の `pyproject.toml`
のみを新規作成して補いました。

### 参考: 中身が全く同じタグ

`v0.0.1`と`v0.0.1a`、`v0.0.3`と`v0.0.3a`は、それぞれ同一コミットで内容が完全に
一致していたため、PyPIには1バージョンずつのみ収録しています(同じ中身を2バージョン
分登録しても無駄になるため)。

## 2. 対応方針と修正内容

- 各タグのソースコード自体は一切変更していません。
- `pyproject.toml` の `version` (存在しない場合は新規作成)と、パッケージ内部の
  `__version__`(`__init__.py` または `_version.py`)を、**そのタグの実体に対応する
  正しいバージョン文字列**に修正しました。
- a/b表記による並び順の矛盾(問題B)を解消するため、各 `0.0.X` 系列内の2番目以降の
  リリースは PEP 440 の post-release表記(`.postN`)に置き換え、**タグが打たれた
  時系列順にバージョンが単調増加する**ようにしました。

### バージョン対応表(Gitタグ → 実際にPyPIへ上げるバージョン)

| PyPI バージョン | 対応する元のGitタグ | 備考 |
|---|---|---|
| ―(対象外) | `v0.0.0` | コードなし。除外 |
| `0.0.0` | `v0.0.0a` | pyproject.tomlを新規作成 |
| `0.0.0.post1` | `v0.0.0b` | |
| `0.0.1` | `v0.0.1`, `v0.0.1a` | 2タグとも同一コミット |
| `0.0.1.post1` | `v0.0.1b` | |
| `0.0.2` | `v0.0.2` | |
| `0.0.2.post1` | `v0.0.2a` | |
| `0.0.2.post2` | `v0.0.2b` | |
| `0.0.3` | `v0.0.3`, `v0.0.3a` | 2タグとも同一コミット |
| `0.0.3.post1` | `v0.0.3b` | |
| `0.0.4` | `v0.0.4` | |
| `0.0.4.post1` | `v0.0.4a` | |
| `0.0.4.post2` | `v0.0.4b` | |
| `0.0.4.post3` | `v0.0.4b1` | |
| `0.0.4.post4` | `v0.0.4b2` | |
| `0.0.4.post5` | `v0.0.4b3` | **最新(HEAD)。`pip install pyside6-webusb`で入るのはこれ** |

## 3. 検証結果(実際にvenv上で実行して確認済み)

`dist/` 内の30ファイル(sdist 15 + wheel 15)全てについて、以下を実機検証しました。

1. `python -m build` でビルド → 15/15 成功
2. PySide6-Essentials / PySide6-Addons / pyusb を入れたvenvへ、15バージョン全てを
   個別に `pip install` → 15/15 成功、`import pyside6_webusb` も正常
3. `importlib.metadata.version(...)` と `__version__` が、上表のバージョンと
   完全一致することを確認
4. 30ファイル全てを1つのローカルインデックスに置いた状態で
   `pip install pyside6-webusb`(バージョン指定なし)を実行 →
   **`0.0.4.post5`(=v0.0.4b3)が選ばれることを確認**(問題Bが解消されている証拠)
5. `pip install pyside6-webusb==0.0.2.post1`(=v0.0.2a)のような個別バージョン
   指定インストールも正常に動作することを確認

## 4. できなかったこと・お願いしたいこと

**実際のPyPIへのアップロードは、こちらでは実行できません。** 理由は次の2点です。

- PyPIへの公開にはあなたのPyPIアカウントのAPIトークンが必要ですが、それは
  お預かりしていません(セキュリティ上、チャットに貼っていただくこともお勧め
  しません)。
- この作業環境からは `upload.pypi.org` への出力アクセスがそもそも許可されて
  いません。

そのため、ビルド済みの30ファイル(`dist/`フォルダ)と、それをアップロードする
ための `publish_to_pypi.sh` を用意しました。ご自身のPyPIアカウントで下記の手順を
実行してください。

### アップロード手順

```bash
# 1. 初めての場合はtwineをインストール
pip install --upgrade twine

# 2. PyPIのAPIトークンを発行(https://pypi.org/manage/account/token/)
#    ※ pyside6-webusb という名前がまだ誰にも登録されていないことは確認済みです

# 3. 一括アップロード(このフォルダのdist/を対象にしています)
bash publish_to_pypi.sh
```

`publish_to_pypi.sh` はトークンをその場で聞くようになっているので、貼り付けて
Enterを押すだけです。**一度PyPIに上げたバージョン番号は、削除しても二度と
再利用できません**。不安であれば、先に [TestPyPI](https://test.pypi.org/) で
一度試すことを強くおすすめします(`publish_to_pypi.sh --test` でTestPyPI向けに
アップロードできます)。

## 5. 今後のタグ打ちについて(任意の提案)

問題A(タグとversionのズレ)は今回だけでなく、`v0.0.4a`以降でも実は起きて
いました(`v0.0.4b2`→`v0.0.4b3`でも1つズレていました)。運用上どうしても
「タグを打ってからversionを上げる」順序になりがちなようなので、今後同じ
ズレが起きないよう、タグ名から自動でバージョンを決める GitHub Actions の
サンプルを `github-actions-publish-pypi.yml` として同梱しました。よければ
`.github/workflows/` に置いて使ってください(不要であれば無視していただいて
問題ありません)。
